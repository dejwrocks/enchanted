{
  "name": "enchanted",
  "version": "0.1.0",
  "description": "Lightweight 2D library for simple games",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --watch src --out-dir dist --source-maps"
  },
  "author": "Wei Lv",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "locutus": "^2.0.5"
  }
}
